<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jeesite.modules.ddwl.dao.DdwlUserAddressDao">
	
	<!-- 查询数据
	<select id="findList" resultType="DdwlUserAddress">
		SELECT ${sqlMap.column.toSql()}
		FROM ${sqlMap.table.toSql()}
		<where>
			${sqlMap.where.toSql()}
		</where>
		ORDER BY ${sqlMap.order.toSql()}
	</select> -->
	<select id="findByUserId" parameterType="java.lang.String" resultType="com.jeesite.modules.ddwl.entity.DdwlUserAddress">
		select * from ddwl_user_address where user_id =  #{userId}
	</select> 
</mapper>